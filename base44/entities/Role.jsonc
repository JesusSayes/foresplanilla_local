{
  "name": "Role",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nombre del rol"
    },
    "description": {
      "type": "string",
      "description": "Descripci\u00f3n del rol"
    },
    "permissions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Lista de permisos asignados al rol"
    },
    "is_system_role": {
      "type": "boolean",
      "default": false,
      "description": "Rol del sistema (no se puede eliminar)"
    },
    "department_restricted": {
      "type": "boolean",
      "default": false,
      "description": "Si es true, solo puede ver datos de su departamento"
    },
    "team_restricted": {
      "type": "boolean",
      "default": false,
      "description": "Si es true, solo puede ver datos de su equipo asignado (managed_team_ids)"
    },
    "site_restricted": {
      "type": "boolean",
      "default": false,
      "description": "Si es true, solo puede ver datos de las sedes permitidas"
    },
    "allowed_sites": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Lista de nombres de sedes que este rol puede visualizar (vac\u00edo = todas)"
    },
    "block_financial_info": {
      "type": "boolean",
      "default": true,
      "description": "Si es true, oculta informaci\u00f3n financiera sensible (salarios, cuentas bancarias, CCI, AFP, etc.)"
    },
    "priority": {
      "type": "integer",
      "default": 0,
      "description": "Prioridad del rol (mayor n\u00famero = mayor prioridad)"
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}